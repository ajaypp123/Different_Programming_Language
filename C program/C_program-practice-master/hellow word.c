//program for hello word 
//preproceser convert machin to binari code

#include<stdio.h>                       // This is the stander input output header file used to call function

int main (void)                     //every program has main function which run first and it rutern value integer 0(void)
                                        //function is set of instruction whichare in brakets
{
	printf("Hello world");               //printf function is in stdio.h used to print & (;) is end of instuction
	getch();                             //it get charector i.e. it wait untill we type chartor otherwise compiler not wait and exit
                                        //it give output (hello world_) & _ this is for get chartor
}
