#include<stdio.h>
#include<conio.h>
void main()
{
	int a,b;
	printf("Enter two num:");
	scanf("%d%d",&a,&b);
	if (a==b)
    {
    	printf("both equal");
    }
    getch();
	
}
//    first cheak condition if it true then after if it wait untill we put char (getch)
//     first cheak condition if it false then it wait untill we put char (getch)
